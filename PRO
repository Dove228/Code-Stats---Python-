# Code Stats 项目结构

本项目是一个功能完整的Python代码统计工具，采用模块化设计，易于扩展和维护。

## 📁 项目目录结构

```
Code-Stats---Python-/
├── codestats_no_pathlib.py   # 单文件版本主程序（可直接运行）
├── CodeStats.bat             # Windows启动脚本（双击运行）
├── .gitignore                # Git忽略配置
├── LICENSE                   # MIT许可证
├── pyproject.toml            # 项目配置文件
├── requirements.txt          # 依赖列表
├── README.md                 # 项目主文档
├── PROJECT_STRUCTURE.md      # 项目结构说明（本文件）
├── build_scripts/            # 打包相关脚本
│   ├── build.bat             # 批处理打包脚本
│   ├── build.py              # Python打包脚本
│   └── BUILD_GUIDE.md        # 构建指南
├── examples/                 # 使用示例
│   └── USAGE.md              # 详细使用说明
├── src/code_stats/           # 源代码目录（开发版）
│   ├── __init__.py           # 包初始化
│   ├── __main__.py           # 模块入口
│   ├── core/                 # 核心模块
│   │   ├── __init__.py
│   │   ├── analyzer.py       # 代码分析器
│   │   ├── complexity.py     # 复杂度分析
│   │   ├── languages.py      # 语言定义(30+语言)
│   │   ├── scanner.py        # 文件扫描器
│   │   └── stats.py          # 统计数据结构
│   ├── exporters/            # 导出器模块
│   │   ├── __init__.py
│   │   ├── json_exporter.py  # JSON导出
│   │   ├── csv_exporter.py   # CSV导出
│   │   └── markdown_exporter.py # Markdown导出
│   └── utils/                # 工具模块
│       ├── __init__.py
│       ├── config.py         # 配置管理
│       ├── git_analyzer.py   # Git分析
│       ├── interactive.py    # 交互式界面
│       └── visualizer.py     # 可视化图表
└── tests/                    # 测试目录
    ├── __init__.py
    └── README.md             # 测试说明
```

## 🧩 模块关系

```
┌─────────────────────────────────────────────────────┐
│                    用户入口                          │
│         CodeStats.bat / __main__.py                 │
└──────────────────────────┬──────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────┐
│                  交互式界面                          │
│              utils/interactive.py                   │
└──────────────────────────┬──────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Scanner    │    │  Analyzer   │    │  Complexity │
│  (扫描器)   │    │  (分析器)   │    │  (复杂度)   │
└──────┬──────┘    └──────┬──────┘    └──────┬──────┘
       │                  │                  │
       ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────┐
│                    Languages                        │
│              core/languages.py (30+语言定义)        │
└─────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────┴──────────────────┐
        ▼                                     ▼
┌─────────────┐                       ┌─────────────┐
│  Exporters  │                       │   Utils     │
│  (导出器)   │                       │  (工具)     │
└─────────────┘                       └─────────────┘
```

## 🔄 数据流

1. **输入**：用户选择功能，输入目标路径
2. **扫描**：`scanner.py` 遍历目录，收集文件信息
3. **分析**：`analyzer.py` 分析文件内容，统计行数
4. **计算**：`complexity.py` 计算代码复杂度（可选）
5. **统计**：`stats.py` 汇总统计结果
6. **输出**：通过 `visualizer.py` 或 `exporters/` 展示或导出

## 📈 扩展性

### 添加新语言支持

1. 在 `core/languages.py` 中添加语言定义
2. 包含扩展名、单行注释、多行注释规则

### 添加新导出格式

1. 在 `exporters/` 目录创建新的导出器类
2. 继承基类并实现导出方法

### 添加新功能

1. 在 `utils/` 或 `core/` 目录创建新模块
2. 在 `interactive.py` 中添加功能菜单

## 🔗 项目地址

GitHub: https://github.com/Dove228/Code-Stats---Python-